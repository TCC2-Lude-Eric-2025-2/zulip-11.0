name: SonarCloud Analysis

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  sonarcloud:
    name: Run SonarCloud Analysis
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout do código
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Necessário para análise correta de histórico pelo SonarCloud

      # 2. Configurar Python
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      # 3. Garantir que a venv do Zulip exista e instalar dependências
      - name: Ensure Zulip virtualenv and install dependencies
        run: |
          echo "🔍 Checking for Zulip virtualenv..."
          if [ ! -d ".venv" ]; then
            echo "⚙️ Creating virtual environment..."
            python -m venv .venv
          fi

          echo "🚀 Activating virtual environment..."
          source .venv/bin/activate
          python -V
          pip install --upgrade pip setuptools wheel
          echo "📦 Installing dependencies from requirements/dev.txt..."
          pip install -r requirements/dev.txt
          pip install pytest pytest-cov mypy ruff

      # 4. Rodar testes backend e gerar coverage.xml
      - name: Run backend tests with coverage
        run: |
          source .venv/bin/activate
          pytest --cov=zerver --cov-report xml:var/coverage.xml

      # 5. Análise estática opcional
      - name: Run static analysis (mypy & ruff)
        run: |
          source .venv/bin/activate
          mypy zerver || true
          ruff check zerver || true

      # 6. SonarCloud Scan
      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@master
        with:
          args: >
            -Dsonar.projectKey=TCC2-Lude-Eric-2025-2_zulip-11.0
            -Dsonar.organization=tcc2-lude-eric-2025-2
            -Dsonar.python.coverage.reportPaths=var/coverage.xml
        env:
          SONAR_CLOUD_TOKEN: ${{ secrets.SONAR_CLOUD_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
